import {assert} from '../lib/util.mjs';
export const cases=[{id:'RPT-CRITICAL-10',domain:'REPEATABILITY',name:'Critical behavior repeatability ×10',critical:true,timeout:15000,isolate:true,async run({a}){for(let k=0;k<10;k++){const sw=a.signal(true),x=a.signal(1),y=a.signal(2),c=a.computed(()=>sw.get()?x.get():y.get());assert(c.get()===1);sw.set(false);assert(c.get()===2);const core=a.createCore();let first=true;core.modules.register({name:'m',init:async()=>{if(first){first=false;throw new Error('x')}},teardown:async()=>{}});try{await core.boot()}catch{}await core.boot();await core.destroy()}return {iterations:10}}}];
