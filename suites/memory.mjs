import {assert} from '../lib/util.mjs';
export const cases=[{id:'M-HEAP-001',domain:'MEMORY',name:'Heap retained delta after 2K lifecycle churn',critical:true,timeout:20000,isolate:true,async run({a}){if(typeof global.gc!=='function')return {classification:'ENVIRONMENT_LIMITED',gc:false};global.gc();const base=process.memoryUsage().heapUsed;for(let i=0;i<2000;i++){const c=a.createCore();await c.destroy()}global.gc();const after=process.memoryUsage().heapUsed;const delta=after-base;assert(delta<16*1024*1024,`retained heap delta ${delta}`);return {baseline:base,afterGC:after,retainedDelta:delta}}}];
